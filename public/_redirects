# SPA catch-all – serves the React app for all client-side routes.
# Netlify automatically serves ANY existing file in the deploy directory BEFORE
# this rule fires (force is false by default), so static assets such as
# /admin/config.yml, /admin/index.html, and /.netlify/* are unaffected.
/* /index.html 200